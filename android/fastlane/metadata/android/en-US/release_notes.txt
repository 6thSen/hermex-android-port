Adds a collapsible scheduled-sessions section with search and a full-list view, recognizes cron-prefixed sessions, and replaces task delivery free text with server-provided choices while preserving custom and legacy targets.
